# Active Context: moe-orchestrator

> **[EVOLUTION_CONTRIBUTION]** (Phase 2 测算声明 / `00-evolution-law.md §1.6`)
> - $F_{System}$: **+** 统筹协调 AIStudioToAPI 稳定性修复、标准化改造与 SR 等级跃迁，确保核心算力网关高可用。
> - $L_{User}$: **+** 自动化排查与降维派单，降低责任主体排障认知负担。
> - $S_{total}$: **+** 规范上下文生命周期与任务激活快照，维持系统负熵。
> - $Value_{Delivered}$: **+** 护航 API 网关稳定服务，保障下游业务持续可用。
> - `ect`: **S** (count(+)=4, count(-)=0, Value_Delivered=+, S_total=+)
> - `verdict`: **KEEP**

## 1. 当前会话状态 (Current Session State)
- **Active Phase**: Phase 2 (Model 404/503 Switch Loop & Thinking-Only Empty Response Mock Fault Tolerance)
- **Task Goal**: 针对模型只思考(Thinking-Only)未给出正文回复的情况实现智能 Mock 答复兜底，覆盖 OpenAI / Claude / Response API / Gemini Native 各流式与非流式通道，并同步至 104 (8317) 容器。
- **Status**: 代码改造与自动化测试全部 100% PASS，已同步至 104 并平滑热重启验证健康状态。

## 2. 核心架构与编排经验 (Orchestration Insights)
1. **Thinking-Only 兜底契约**: 当模型仅输出 thought/reasoning 且未输出任何正文或工具调用时，必须在各协议输出端安全注入 `[Thinking completed, but no text response was generated by the model.]` Mock 正文，防止下游客户端呈现空白或因缺失 content 字段解析异常。
2. **正常响应防误触**: 必须严格保障当模型有正常 text 输出或 functionCall/toolCall 时，Mock 兜底完全不介入，保留模型原生内容与完整 reasoning_content。
3. **多协议状态机统一**: OpenAI SSE 流、Claude SSE 流、OpenAI Response API 以及 Gemini Native 分块均需在 `finishReason` 到达且正文未发送时统一触发 Mock 补齐。
